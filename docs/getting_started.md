# Getting Started

1. Create a virtual environment and activate it.
2. Install dependencies from `requirements.txt`.
3. Configure `src/text2sql/config/config.yaml` with model and API keys.
4. Run `python examples/text2sql_demo.py` to see the placeholder demo.

More detailed instructions will be added as implementation progresses.
